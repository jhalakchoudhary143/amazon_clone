#include<stdio.h>
#include<stdlib.h>
void main()
{
     int n,k,a,b;
     printf("Enter the  size of array");
     scanf("%d",&n);
     int arr[n];
     printf("enter the elements of the array");
     for(int i=0;i<n;i++)
     {
        scanf("%d",&arr[i]);
     }
     printf("Enter a and b btween which we have to find minimum distance");
     scanf("%d",&a);
     scanf("%d",&b);
     
}